# 备忘

- 结构：6 层 / 512 / RoPE / RMSNorm，词表和 GPT-2 一样所以能 KL。
- cache 每层 `(B, 8, T, 64)`。RoPE 要用绝对位置，不然和整段前向对不上。满 256 后整窗重算，不是报错停住。
- 蒸馏老师是网页 gpt2，莎士比亚 val 上 PPL 62，学生 33。T=4 到 30.5；α=0 只继续 CE 停在 33.5；T=1 变差。
- INT8 只压 Linear，词嵌入还是 fp32，所以体积不是四分之一。量化后可以走 KV-Cache。
- vLLM 0.8.5.post1 才能配现在的 torch；eager 是因为本机没 C 编译器。8 条快主要是批处理，单条大约 1.4 倍。
- `lm_head` 没绑 `wte`。绑了现有 ckpt 对不上。
- `chat_cli.py` 把多轮拼成一条上下文，不是指令对话模板。
- 没做电商数据。莎剧续训到评论对不上词表和领域。
